<section class="py-20 px-4 sm:px-6 border-t border-border">
  <div class="max-w-6xl mx-auto">
    <h2 class="text-3xl sm:text-4xl font-bold text-center mb-4">
      The ops layer for your agent fleet.
    </h2>
    <p class="text-text-muted text-center max-w-2xl mx-auto mb-12">
      Structure your agents like a team. Let them run autonomously. Step in when it matters.
    </p>

    <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6">
      <div class="p-6 rounded-lg bg-bg-card border border-accent/30">
        <div class="flex items-center gap-3 mb-3">
          <div class="w-8 h-8 rounded-md bg-accent/20 flex items-center justify-center">
            <span class="text-accent text-sm font-mono font-bold">T</span>
          </div>
          <h3 class="font-semibold">Team Topology</h3>
        </div>
        <p class="text-text-muted text-sm leading-relaxed">
          Define team structures for your agents — leads, sub-agents, roles. Assign a lead to plan architecture while sub-agents write code and run tests. Marmy gives your fleet a shape instead of a flat list of sessions.
        </p>
      </div>

      <div class="p-6 rounded-lg bg-bg-card border border-accent/30">
        <div class="flex items-center gap-3 mb-3">
          <div class="w-8 h-8 rounded-md bg-accent/20 flex items-center justify-center">
            <span class="text-accent text-sm font-mono font-bold">&#x21BB;</span>
          </div>
          <h3 class="font-semibold">Autonomous Scheduling</h3>
        </div>
        <p class="text-text-muted text-sm leading-relaxed">
          Set trusted agents to run on a schedule — cron for your AI team. A nightly code review agent, a weekly dependency updater, a continuous test runner. Define the cadence, let them execute.
        </p>
      </div>

      <div class="p-6 rounded-lg bg-bg-card border border-border">
        <div class="flex items-center gap-3 mb-3">
          <div class="w-8 h-8 rounded-md bg-accent/20 flex items-center justify-center">
            <span class="text-accent text-sm font-mono font-bold">+</span>
          </div>
          <h3 class="font-semibold">Multi-Machine Fleet</h3>
        </div>
        <p class="text-text-muted text-sm leading-relaxed">
          Work laptop, home server, cloud VM — add every machine to your fleet. One app shows every agent across every machine. No SSH, no terminal tabs, no remembering what's running where.
        </p>
      </div>

      <div class="p-6 rounded-lg bg-bg-card border border-border">
        <div class="flex items-center gap-3 mb-3">
          <div class="w-8 h-8 rounded-md bg-accent/20 flex items-center justify-center">
            <span class="text-accent text-sm font-mono font-bold">&gt;_</span>
          </div>
          <h3 class="font-semibold">Live Session View</h3>
        </div>
        <p class="text-text-muted text-sm leading-relaxed">
          Tap into any agent's session to see live output, approve or reject actions, and send follow-up prompts. Full terminal interaction from a native mobile interface.
        </p>
      </div>

      <div class="p-6 rounded-lg bg-bg-card border border-green/30">
        <div class="flex items-center gap-3 mb-3">
          <div class="w-8 h-8 rounded-md bg-green/20 flex items-center justify-center">
            <span class="text-green text-sm font-mono font-bold">ts</span>
          </div>
          <h3 class="font-semibold">Tailscale Zero-Config</h3>
        </div>
        <p class="text-text-muted text-sm leading-relaxed">
          Already on Tailscale? Marmy auto-detects it. Add machines anywhere on your tailnet — no port forwarding, no VPN config, no manual IP entry.
        </p>
      </div>

      <div class="p-6 rounded-lg bg-bg-card border border-green/30">
        <div class="flex items-center gap-3 mb-3">
          <div class="w-8 h-8 rounded-md bg-green/20 flex items-center justify-center">
            <span class="text-green text-sm font-mono font-bold">🔒</span>
          </div>
          <h3 class="font-semibold">Secure by Default</h3>
        </div>
        <p class="text-text-muted text-sm leading-relaxed">
          All connections encrypted end-to-end. No cloud relay — your data never leaves your network. Pairing uses one-time keys with Ed25519 authentication.
        </p>
      </div>
    </div>
  </div>
</section>
