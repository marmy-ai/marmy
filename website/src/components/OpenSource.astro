<section id="get-started" class="py-24 sm:py-32 px-5 sm:px-8 section-divide">
  <div class="max-w-2xl mx-auto">
    <h2 class="text-2xl sm:text-3xl font-bold sm:text-center mb-4 tracking-[-0.02em] reveal">
      Open source. Self-hosted. Yours.
    </h2>
    <p class="text-text-secondary text-lg leading-relaxed max-w-lg sm:mx-auto sm:text-center mb-10 reveal stagger-1">
      The agent, the app, and the relay are all MIT-licensed. Run it on your own hardware. Audit every line. No vendor lock-in, no cloud dependency, no subscription.
    </p>

    <!-- Get started steps -->
    <div class="space-y-4 reveal stagger-2">
      <div class="p-6 rounded-xl bg-bg-card border border-border hover:border-border-light transition-colors">
        <div class="text-primary font-mono text-xs mb-3 tracking-wider">01</div>
        <h3 class="font-semibold mb-2">Download MacMarmy (macOS)</h3>
        <p class="text-text-secondary text-lg leading-relaxed mb-3">Download the agent, open it, it runs in your menu bar.</p>
        <a href="https://github.com/marmy-ai/marmy/releases/latest" target="_blank" rel="noopener"
          class="inline-flex items-center gap-1 text-primary text-sm hover:underline">Download latest release &rarr;</a>
      </div>

      <div class="p-6 rounded-xl bg-bg-card border border-border hover:border-border-light transition-colors">
        <div class="text-primary font-mono text-xs mb-3 tracking-wider">02</div>
        <h3 class="font-semibold mb-2">Get the iOS app</h3>
        <p class="text-text-secondary text-lg leading-relaxed mb-3">Download the app, pair with your machine from the menu bar.</p>
        <a href="https://testflight.apple.com/join/v8HmNu1H" target="_blank" rel="noopener"
          class="inline-flex items-center gap-1 text-primary text-sm hover:underline">Join iOS Beta &rarr;</a>
      </div>
    </div>

    <p class="text-text-secondary sm:text-center text-sm leading-relaxed max-w-lg sm:mx-auto mt-8 mb-8 reveal stagger-3">
      Want to build from source or run on Linux? Check out the repo.
    </p>

    <div class="flex flex-col sm:flex-row gap-3 sm:justify-center reveal stagger-3">
      <a href="https://github.com/marmy-ai/marmy" target="_blank" rel="noopener"
        class="inline-flex items-center justify-center gap-2 px-7 py-3 rounded-lg bg-primary hover:bg-primary-hover text-white font-semibold transition-all hover:shadow-lg hover:shadow-primary/20">
        View on GitHub
      </a>
      <a href="https://github.com/marmy-ai/marmy#readme" target="_blank" rel="noopener"
        class="inline-flex items-center justify-center gap-2 px-7 py-3 rounded-lg border border-border text-text-primary hover:bg-bg-card hover:border-border-light transition-all">
        Read the Docs
      </a>
    </div>
  </div>
</section>

<script>
  document.querySelectorAll('.copy-btn').forEach((btn) => {
    btn.addEventListener('click', async () => {
      const code = btn.getAttribute('data-code');
      if (!code) return;
      await navigator.clipboard.writeText(code);
      const original = btn.textContent;
      btn.textContent = 'copied!';
      setTimeout(() => { btn.textContent = original; }, 2000);
    });
  });
</script>
